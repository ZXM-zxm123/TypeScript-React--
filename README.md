# TypeScript-React--
前端 React + TypeScript 实现代码编辑器（Monaco Editor）、视频窗口、聊天面板。Node.js 使用 Socket.io 管理房间、代码同步、光标位置同步。Go 服务提供代码执行接口（接收代码和输入，返回输出），通过 gRPC 或 HTTP 与 Node.js 通信。支持常见语言（JavaScript、Python）。面试结束可保存代码和对话记录到数据库。实现房间码、面试官/候选人身份、录制回放。输出前端、Node.js、Go 三端代码。
